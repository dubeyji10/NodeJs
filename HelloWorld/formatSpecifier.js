/*
%s format a variable as a string
%d format a variable as a number
%i format a variable as its integer part only
%o format a variable as an object
*/
let myNum = Number(12321);
console.log('%o', myNum);
console.log('My %s has %d ears', 'dog', 2);
